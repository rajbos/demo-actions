name: Process Ids
on: 
  push:
    paths:
    - .github/workflows/process-ids.yml
    
  workflow_dispatch:
  
jobs:
  test-job:
    runs-on: ubuntu-latest
    steps:
    - run: echo $PID
    - run: echo $PID
    - run: echo $PID
    - run: echo $PID
    - run: echo $PID
